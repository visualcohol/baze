{
  "name": "baze",
  "version": "1.0.0",
  "description": "Bazic elements of a reactive website architecture",
  "main": "src/index.js",
  "scripts": {
    "start": "rm -rf .cache dist && parcel src/index.html",
    "build": "rm -rf .cache dist && parcel build src/index.js --no-cache"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.6.0",
    "eslint-config-prettier": "^6.11.0",
    "parcel-bundler": "^1.12.4"
  }
}
